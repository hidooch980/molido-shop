services:
  - type: web
    name: molido-accounting-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: DB_DATABASE
        value: /tmp/molido-accounting.sqlite
